# ✅ SSH восстановлен!

## Что было сделано

1. **SSH работает!** Проблема была в том, что на сервере используется сервис `ssh` вместо `sshd`
2. **SSH демон запущен и включён в автозапуск**
3. **Nginx работает**
4. **PM2 запущен**, но приложение `like-that` требует установки зависимостей

## Текущий статус

- ✅ SSH работает (порт 22 открыт, сервис `ssh` активен)
- ✅ Nginx работает
- ⚠️ Next.js приложение требует установки зависимостей (`npm ci`)

## Что нужно сделать дальше

### Вариант 1: Через SSH (рекомендуется)

```bash
cd "/Users/egorgalij/Desktop/projects macbook/configurator"
./scripts/fix-domain-with-password.sh
```

Этот скрипт:
1. Установит зависимости (`npm ci`)
2. Сгенерирует Prisma клиент
3. Соберёт приложение (`npm run build`)
4. Запустит через PM2

### Вариант 2: Через GitHub Actions

**Actions** → **Fix via File Upload** → **Run workflow**

Workflow теперь использует правильное имя сервиса (`ssh` вместо `sshd`) и установит зависимости.

### Вариант 3: Вручную по SSH

```bash
ssh root@130.49.149.162
cd /var/www/like-that
npm ci
npx prisma generate
npm run build
pm2 restart like-that
pm2 save
```

## Важно

На этом сервере используется сервис **`ssh`** (не `sshd`). Все скрипты обновлены для использования правильного имени.

## Проверка

После установки зависимостей проверьте:

```bash
curl http://app.ubernatural.io/api/admin/health
```

Должен вернуться JSON с информацией о здоровье приложения, а не HTML или 502.
